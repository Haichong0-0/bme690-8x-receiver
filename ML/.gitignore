# Raw capture CSVs — user-specific, not for source control (moved here from
# Server/'s old data/ — see build_dataset.py's module docstring)
data/raw/

# Generated by build_dataset.py — regenerate, don't commit
data/processed/
data/diagnostics/
data/diagnostics_const320/
data/diagnostics_eval/

# Generated by train.py — regenerate, don't commit
models/
# NOT ignored: experiments.csv / EXPERIMENTS.md — also generated by train.py,
# but they accumulate history across runs rather than being disposable
# rebuild output, so they're meant to persist/be committed.

__pycache__/
*.pyc
experiments.pending.jsonl

# testing/ tool output (regenerable)
testing/output/
